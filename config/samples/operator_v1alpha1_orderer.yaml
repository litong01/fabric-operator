apiVersion: operator.fabric.hyperledger.org/v1alpha1
kind: Orderer
metadata:
  name: orderer-sample
spec:
  # Add fields here
  organization: example.com
  nodeCert:
  release: 2.3.2
  storage: 5Gi
  logLevel: DEBUG