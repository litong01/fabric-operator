apiVersion: operator.fabric.hyperledger.org/v1alpha1
kind: CA
metadata:
  name: ca-sample
spec:
  # Add fields here
  organization: example.com
  nodeCert:
  release: 1.5.0
  storage: 5Gi
  logLevel: DEBUG