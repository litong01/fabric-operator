apiVersion: v1
kind: ConfigMap
metadata:
  name: fabric-tcp-services
  namespace: {{ theconfigmap.resources[0].metadata.namespace }}
data:
{% for aport in toberemoved %}
  {{ aport.key|int }}:
  $patch: delete
{%  endfor %}